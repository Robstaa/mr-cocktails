<h1><%= @cocktail.name %></h1>
<% @cocktail.doses.each do |dose| %>
  <p><%= "#{dose.description} | #{dose.ingredient.name}" %></p>
<% end %>

<h3><%= link_to("Add new ingredients", new_cocktail_dose_path(@cocktail)) %></h3>
<h3><%= link_to("Go back to overview", root_path) %></h3>
